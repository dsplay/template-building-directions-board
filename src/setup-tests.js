import '@testing-library/jest-dom/vitest';

window.dsplay_config = {
  locale: 'en',
  orientation: 'landscape',
};

window.dsplay_media = {
  duration: 30000,
  targets: [
    {
      name: 'Example', place: 'Floor 1', floor: 'Ground floor', direction: 'left', logo: '',
    },
  ],
};
